import { NgModule } from '@angular/core';
import { ScrollableListDirective } from './scrollable-list.directive';

@NgModule({
    imports: [],
    exports: [ScrollableListDirective],
    declarations: [ScrollableListDirective],
    providers: [],
})
export class DirectivesModule { }
